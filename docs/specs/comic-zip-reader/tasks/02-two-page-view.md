# 02 — 두 장 보기(스프레드) 모드

## Outcome

한 장 보기와 두 장 보기를 언제든 전환할 수 있고, 두 장 보기에서는 1페이지가 표지로 단독
표시된 뒤 2-3, 4-5 순으로 짝지어 보인다. 좌/우 클릭 넘김과 방향 설정은 두 장 보기에서도
한 장 보기와 동일한 규칙으로 동작한다.

## Blockers

01 (기본 읽기 경험) — 전환 대상이 되는 읽기 화면과 넘김/방향 로직이 있어야 한다.

## Acceptance criteria

- [x] 보기 모드 전환 컨트롤로 한 장 ↔ 두 장 보기를 즉시 전환할 수 있다.
- [x] 두 장 보기 진입 시 1페이지가 단독으로 표시된다.
- [x] 이후 스프레드는 2-3, 4-5 순으로 짝지어 표시된다.
- [x] 두 장 보기에서도 좌/우 클릭 넘김과 방향 설정이 한 장 보기와 동일한 규칙으로 동작한다.

## Constraints

None.

## Verification

- 짝수/홀수 페이지 수를 가진 zip 픽스처로 스프레드 짝짓기와 좌우 클릭 동작을 자동화 테스트로 확인.

## Review checkpoint

None.

## Status

completed

## Execution

- Verification: `bun run typecheck` 통과, `vitest run` 5개 파일 26개 테스트 통과
  (lib/comic-spread.test.ts 신규 8개 포함). 실행 중인 dev 서버에서 실제 zip 업로드 후
  두 장 보기로 전환 → 1페이지 단독 표시 → 다음 장 클릭 시 "2-3 / 3"으로 2·3페이지가
  함께 표시(기본 오→왼 방향에서 3쪽이 왼쪽, 2쪽이 오른쪽)되는 것과, 이전 장 클릭 시
  표지로 돌아가는 것, 한 장 보기로 되돌아가면 1페이지로 리셋되는 것을 브라우저에서
  직접 확인. 콘솔 에러 없음.
- Blocker: —
- Revision: —
