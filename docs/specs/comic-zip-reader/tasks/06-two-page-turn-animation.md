# 06 — 두 장 보기에서 애니메이션 지원(낱장 독립 커링)

## Outcome

두 장 보기에서도 페이지 넘김 애니메이션이 동작하며, 넘어가는 쪽 페이지만 낱장처럼
독립적으로 굽고 반대쪽 페이지는 그대로 유지된다. 방향 일치, 재생 중 즉시 클릭 반영,
켜기/끄기 설정은 한 장 보기와 동일하게 적용된다.

## Blockers

05 (페이지 넘김 애니메이션 — 한 장 보기) — 라이브러리 도입과 기본 넘김 애니메이션
로직이 먼저 있어야 한다.
02 (두 장 보기) — 애니메이션을 적용할 스프레드 화면이 있어야 한다. (완료됨)

## Acceptance criteria

- [x] 두 장 보기에서 좌/우 클릭 시에도 애니메이션이 재생된 뒤 페이지(스프레드)가
      바뀐다.
- [x] 넘어가는 쪽 페이지만 낱장처럼 독립적으로 굽고, 반대쪽 페이지는 그대로
      유지된다.
- [x] 애니메이션 방향, 재생 중 즉시 클릭 반영, 켜기/끄기 설정이 한 장 보기와
      동일하게 적용된다.

## Constraints

None.

## Verification

- 자동화 테스트로 두 장 보기에서의 애니메이션 동작을 확인하고, 실행 중인 dev
  서버에서 실제 넘김을 시각적으로 확인.

## Review checkpoint

None.

## Status

completed

## Execution

- Verification: `bun run typecheck` 통과, `vitest run` 7개 파일 56개 테스트 통과
  (comic-reader.test.tsx에 표지→스프레드 전환 시 전체가 굽는 것, 스프레드↔스프레드
  전환 시 넘어가는 쪽 페이지만 오버레이로 굽고 그 오버레이의 이미지가 실제로
  방금까지 보이던 페이지와 같은 것, 두 장 보기에서 애니메이션을 꺼도 순간
  전환되는 것, 애니메이션 재생 중 다른 zip을 열어도 이전 오버레이가 남지 않는 것
  4개 테스트 추가). 실행 중인 dev 서버에서 7페이지 zip으로 표지→2-3쪽(슬롯 full)
  → 4-5쪽(슬롯 left, 오버레이가 정확히 왼쪽 절반 폭만 차지) → 다시 2-3쪽(이전 장,
  슬롯 right)까지 실제로 넘기며 각 단계의 slot/direction과 페이지 텍스트가 즉시
  반영되는 것을 직접 확인. 콘솔에 남은 404는 이전 작업의 진단성 로그로 이번
  흐름과 무관함을 확인.
- Blocker: —
- Revision: `code-review low` 통과 중 `reset()`이 진행 중이던 넘김 오버레이를
  지우지 않는 결함을 발견 — 애니메이션 재생 중 "다른 zip 업로드" 후 곧바로 새
  zip을 열면 이전 zip의 조각 이미지가 잠시 겹쳐 보일 수 있었다. 실제 dev
  서버에서 타이밍을 정밀 측정해 재현 여지를 확인했고(오버레이 정리 폴백
  타이머와의 근소한 경합), 회귀 테스트를 먼저 작성해 수정 전 실패·수정 후 통과를
  확인한 뒤 `reset()`에 `setOverlay(null)`을 추가해 고쳤다.
